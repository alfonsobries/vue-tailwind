export enum HideReason {
  Outside = 'outside',
  Close = 'close',
  Esc = 'esc',
  Cancel = 'cancel',
  Ok = 'ok',
}

export enum DialogType {
  Alert = 'alert',
  Confirm = 'confirm',
  Prompt = 'prompt',
}

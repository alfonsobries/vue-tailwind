export enum HideReason {
  Outside = 'outside',
  Close = 'close',
  Esc = 'esc',
  Cancel = 'cancel',
  Ok = 'ok',
  Method = 'method',
}

export enum DialogType {
  Alert = 'alert',
  Confirm = 'confirm',
  Prompt = 'prompt',
}
